[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "loom"
version = "0.1.0.dev0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["loom*"]
